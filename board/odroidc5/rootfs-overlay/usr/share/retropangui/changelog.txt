RetroPangui 0.16

[ES / UI]
- 자동 게임 컬렉션(ALL GAMES, FAVORITES, RANDOM, LAST PLAYED) 기본 활성화
- 공장 초기화 메뉴 추가 (시스템 설정 > 고급 설정)
- 설정 항목 제자리 이동: VRAM 제한·프레임 표시 → UI 설정, 절전 모드 → 시스템 설정, 백그라운드 인덱싱 → 게임 설정
- ES VRAM 제한 / ES 프레임 표시로 레이블 명확화
- 외부 저장장치 감지 및 share 선택 GUI 추가
- 저장장치 선택 메뉴에 INTERNAL(내장 eMMC) 항목 추가
- ES 종료 메뉴 제거 (shutdown/reboot 중복 호출 버그 수정)
- 에뮬레이터 설정 메뉴 레이블 단순화 (짧은 시스템 ID)
- 외부 저장장치 감지 팝업 문구 개선
- VIDEO DRIVER 메뉴 추가 (Vulkan / OpenGL ES)

[OTA / 빌드]
- initramfs OTA 업데이트 지원 — squashfs와 함께 initramfs.cpio.gz 배포
- OTA 빌드 시 rootfs-overlay 미적용 버그 수정
- OTA 적용 후 upper/etc/init.d 자동 정리 (구버전 init 스크립트가 squashfs를 가리는 버그 수정)
- 공장 초기화 플래그(/boot/.factory-reset) 처리 — upper 레이어 전체 초기화 후 재부팅
- ota.sh push 전 squashfs magic bytes 검증 추가
- build.sh: --img / --ota 독립 플래그, 기본값 둘 다 출력

[시스템]
- 최초 부팅 시 Mega Drive / MSX1 / MSX2 / ScummVM 추가 ROM 백그라운드 다운로드
- poweroff 재부팅 버그 수정 (halt -f + watchdog 정지)
- beetle-psx-hw Vulkan 전용 빌드 (GLES 전용 플랫폼 호환)
- share 파티션 관리 재설계 (boot.conf 기반, 외부 저장장치 지원)

[컨트롤러]
- gamepad-mgr: 동글 재연결 시 커서 드리프트 수정
- gamepad-mgr: 이중 등록 및 조이스틱 감지 오류 수정
