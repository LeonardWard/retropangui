# retropangui_features.yml
# ES 시작 시 이 파일을 읽어 메뉴 항목을 자동 생성한다.
# 값은 retropangui.conf 에 저장된다.
# restart: none | es | system

menus:
  - id: system_settings
    label: "SYSTEM SETTINGS"
    parent: main
    items:
      # LANGUAGE 항목은 제거됨 — system.language는 ES 언어와 무관해서
      # "바꿔도 한국어가 안 나오는" 혼란만 유발. ES 언어는 UI SETTINGS의
      # LANGUAGE 메뉴(emulationstation.Language, conf 역기록 지원)로 변경.
      - id: rp.timezone
        label: "TIMEZONE"
        type: list
        conf_key: system.timezone
        restart: system
        options:
          - value: Asia/Seoul
            label: "Seoul (KST)"
          - value: America/New_York
            label: "New York (EST)"
          - value: Europe/Paris
            label: "Paris (CET)"
          - value: America/Los_Angeles
            label: "Los Angeles (PST)"

  - id: network_settings
    label: "NETWORK SETTINGS"
    parent: main
    items:
      - id: rp.ssh
        label: "SSH"
        type: toggle
        conf_key: system.ssh
        restart: system

  - id: video_settings
    label: "VIDEO SETTINGS"
    parent: main
    items:
      - id: rp.video_smooth
        label: "SMOOTH SCALING"
        type: toggle
        conf_key: global.video_smooth
        restart: none

      - id: rp.video_scale_integer
        label: "INTEGER SCALING"
        type: toggle
        conf_key: global.video_scale_integer
        restart: none

  - id: game_settings
    label: "GAME SETTINGS"
    parent: main
    items:
      - id: rp.rewind
        label: "REWIND"
        type: toggle
        conf_key: global.rewind_enable
        restart: none

      - id: rp.savestate_auto_save
        label: "AUTO SAVE STATE"
        type: toggle
        conf_key: global.savestate_auto_save
        restart: none

      - id: rp.savestate_auto_load
        label: "AUTO LOAD STATE"
        type: toggle
        conf_key: global.savestate_auto_load
        restart: none

  - id: advanced_settings
    label: "ADVANCED SETTINGS"
    parent: main
    items:
      - id: rp.input_joypad_driver
        label: "JOYPAD DRIVER"
        type: list
        conf_key: global.input_joypad_driver
        restart: system
        options:
          - value: linuxraw
            label: "linuxraw (기본)"
          - value: udev
            label: "udev"

      - id: rp.menu_unified_controls
        label: "UNIFIED CONTROLS"
        type: toggle
        conf_key: global.menu_unified_controls
        restart: none
